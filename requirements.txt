# Minimal dependencies for the grade-level-drift pipeline.
# Pin major versions where API stability matters; let pip resolve the rest.

# HTTP and config
httpx>=0.27
python-dotenv>=1.0

# Instruction generation + standard rewriting (only LLM step in the pipeline)
openai>=1.50

# Deterministic evaluation stack — no LLM-as-judge in v0
textstat>=0.7        # Flesch-Kincaid, SMOG, Coleman-Liau, ARI, Gunning Fog, Dale-Chall, ...
spacy>=3.7           # dependency parsing for syntactic complexity features
scikit-learn>=1.4    # optional CLEAR-calibrated grade-level regressor
pyphen>=0.14         # syllable counting (textstat dependency)

# Analysis + interactive reporting
pandas>=2.2
numpy>=1.26
scipy>=1.13
statsmodels>=0.14    # OLS trendlines in plotly express scatter (report.py)
plotly>=5.20         # interactive HTML visualizations
pyarrow>=15.0        # parquet I/O for per-output scores

# Notebooks (kept for exploratory work; canonical logic lives in src/)
jupyter>=1.0
ipykernel>=6.29

# --- non-Python dependencies ---
# Quarto (https://quarto.org) is a separate binary, not pip-installable.
# Used to render docs/research-proposal.qmd to HTML/PDF.
#   macOS:    brew install --cask quarto
#   Linux:    download .deb / .rpm from quarto.org/docs/get-started/
# Render via:  bin/render
#
# spaCy English model — install once after pip install:
#   python -m spacy download en_core_web_sm
